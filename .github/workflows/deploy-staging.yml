name: Deploy to Staging

on:
  push:
    branches: [staging]

jobs:
  deploy:
    name: Deploy Risk Profiling to Staging
    runs-on: [self-hosted, staging]
    steps:
      - name: Run staging deploy script
        run: bash /var/www/staging/risk-profiling/scripts/deploy-staging.sh
